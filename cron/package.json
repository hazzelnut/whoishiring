{
  "name": "cron",
  "version": "1.0.0",
  "description": "a cron job to ingest data into postgres",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "schedule:ingest": " npx tsx --no-cache -r dotenv/config ./cron.ts"
  },
  "dependencies": {
    "dotenv": "^16.1.4",
    "drizzle-orm": "^0.26.5",
    "html-to-text": "^9.0.5",
    "node-cron": "^3.0.2",
    "p-limit": "^4.0.0",
    "postgres": "^3.3.5",
    "tsx": "^3.12.7"
  }
}
